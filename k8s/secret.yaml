# Optional: API key secret for authenticated access
# Create with:
#   kubectl create secret generic infomesh-secret \
#     --from-literal=api-key=YOUR_KEY_HERE \
#     -n infomesh
apiVersion: v1
kind: Secret
metadata:
  name: infomesh-secret
  namespace: infomesh
type: Opaque
stringData:
  api-key: ""  # Set your API key here or via kubectl
